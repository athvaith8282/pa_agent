version: '3.8'

services:
  pa-agent:
    build: .
    ports:
      - "8501:8501"
    environment:
      
      # Streamlit configuration
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
      
      # Python configuration
      - PYTHONPATH=/app
    volumes:
      # Persist data between container restarts
      - ./data:/app/data
      - ./logs:/app/logs
      - ./retriever/data:/app/retriever/data
      # Mount secrets configuration
      - ./.streamlit:/app/.streamlit
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - pa-agent-network

networks:
  pa-agent-network:
    driver: bridge

# Optional: Uncomment if you want to use a separate database service
# volumes:
#   pa-agent-data:
#     driver: local
